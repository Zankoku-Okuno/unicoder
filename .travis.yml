language: haskell

install:
    - sudo cabal install stack
script:
    - stack init
    - stack --resolver lts-8 --install-ghc test


notifications:
    email:
        on_success: never
on_failure: change